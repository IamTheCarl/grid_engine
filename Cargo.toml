[workspace]
members = ["common", "client", "client/gpu_code", "server"]

# Release build uses every possible optimization.
[profile.release]
codegen-units = 1
incremental = false
lto = true
opt-level = 3
